require 'rubygems'
require 'cucumber'
require 'cucumber/rake/task'

Cucumber::Rake::Task.new(:all) do |t|
  t.profile = 'all'
end

Cucumber::Rake::Task.new(:user_login) do |t|
  t.profile = 'user_login'
end
